# Place multi-speaker 6-language base ONNX here to enable base_6lang voice:
#   model.onnx
#   config.json
#
# Official ayousanz/piper-plus-base currently publishes FT checkpoint only
# (speaker embeddings removed). When multilingual-6lang-*.onnx is published,
# copy it here and rebuild: docker compose build tts
